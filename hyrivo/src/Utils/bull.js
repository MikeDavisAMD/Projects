const { Box } = require("@mui/material");

export const bull = (
    <Box
      component="span"
      sx={{ display: 'inline-block', mx: '2px', transform: 'scale(1.08)' }}
    >
      •
    </Box>
  );